import { App } from '../world/CustomWorld';
import {Given, When, Then} from '@cucumber/cucumber'

When('the user enters the {string} search key in search box', async function (this: App, string) {
  
});

When('click the search button', async function (this: App) {
  
});

Then('the user navigates to the product page', async function (this: App) {
  
});

Then('the related products should be displayed', async function (this: App) {
  
});

Then('the user should see the mesage as {string}', async function (this: App, string) {
  
});